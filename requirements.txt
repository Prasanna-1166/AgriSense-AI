# AgriSense AI - Agricultural Decision-Support Platform
# Install with: pip install -r requirements.txt
#
# Compatible with Python 3.12 on Windows, macOS, and Linux.
#
# No API keys are required to run this app:
#   - Geocoding uses the free OpenStreetMap Nominatim service
#   - Weather/climate/elevation data uses the free, keyless Open-Meteo API
#   - Soil data uses the free, keyless SoilGrids/ISRIC REST API (with
#     graceful fallback to regional estimates if unavailable)
#
# Optional environment variables (see .env.example):
#   AGRISENSE_HOST, AGRISENSE_PORT, AGRISENSE_DEBUG, AGRISENSE_FORCE_RETRAIN

Flask==3.0.3
requests==2.32.3
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
joblib==1.4.2
pytest==8.3.2

# --- AgriSense AI V2 additions ---
# pypdf: text extraction from uploaded PDF soil test reports (optional
# feature - the app degrades gracefully to manual entry if this or the
# OCR libraries below are unavailable)
pypdf==4.3.1

# Optional image OCR for soil report photo uploads. These are heavier
# dependencies (Pillow + Tesseract wrapper); image extraction simply
# reports "unavailable, please enter manually" if not installed, so you
# may comment these two lines out if you only need PDF support.
Pillow==10.4.0
pytesseract==0.3.13
gunicorn==23.0.0
